# fedupdate
Script de mise à jour de Fedora Linux basée sur dnf et flatpak.

## comment installer fedupdate ?
```git clone https://codeberg.org/jotenakis/fedupdate && cd fedupdate && chmod +x ./install.sh && ./install.sh```


